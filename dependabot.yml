version: 1
updates:
  # Enable version updates for gem
  - package-ecosystem: "gem"
    # Look for `Gemfile` and `lock` files in the `root` directory
    directory: "/"
    # Check for updates every day (weekdays)
    schedule:
      interval: "daily"
